// Container Variables

--container-width ?= {
  xs: 100%
  sm: --breakpoints['sm'] - --gutters['sm']
  md: --breakpoints['md'] - --gutters['md']
  lg: --breakpoints['lg'] - --gutters['lg']
  xl: --breakpoints['xl'] - --gutters['xl']
}